# PLAN: 単位換算ツール

## 1. 概要

長さ・重さ・温度の3カテゴリに対応した単位換算の静的単一ページWebアプリを作る。ユーザーはカテゴリを選択し、2つの単位（変換元・変換先）をドロップダウンで選ぶ。どちらか一方の入力欄に数値を入れると、もう一方の欄に変換結果がリアルタイム（input イベント）で反映される双方向換算を提供する。サーバコード・外部API・ビルドツールは使わず、`public/index.html` 1ファイル（CSS/JSインライン）で完結させる。

## 2. 受け入れ条件

- [ ] カテゴリ選択UIに「長さ」「重さ」「温度」の3つが表示され、切り替えるとカテゴリに応じた単位一覧（長さ: mm/cm/m/km/インチ/フィート等、重さ: mg/g/kg/トン/ポンド等、温度: ℃/℉/K）が両側の単位セレクトに反映される
- [ ] 左側の入力欄に数値を入力すると、右側の欄に正しく変換された値が即座（input イベント）に表示される（例: 長さ 1 m → 100 cm、重さ 1 kg → 1000 g）
- [ ] 右側の入力欄に数値を入力した場合も、左側の欄に逆方向の変換結果が即座に反映される（双方向換算）
- [ ] 温度カテゴリでオフセットを含む変換が正しい（例: 0 ℃ → 32 ℉、100 ℃ → 212 ℉、0 ℃ → 273.15 K）
- [ ] 入力欄が空、または数値として解釈できない値の場合、エラー表示やNaNにならず、対向の欄が空（プレースホルダー表示）になる
- [ ] `npm run verify` が成功する

## 3. 実装方針

### 構成・技術

- `public/index.html` 単一ファイル。CSS は `<style>`、JS は `<script>` にインラインで記述する。フレームワーク・ライブラリは使わない（Vanilla JS）
- favicon は `<link rel="icon" href="data:image/svg+xml,...">` のインライン data URI で埋め込む。絵柄はアプリのテーマに合わせた「⇄（双方向矢印）と定規」風のSVGにする
- 変更するファイルは `public/index.html` のみ（README.md は保持）

### レイアウト

```
┌──────────────────────────────┐
│  単位換算ツール（タイトル）        │
│  [長さ] [重さ] [温度]  ← カテゴリタブ │
│                              │
│  [数値入力A] [単位セレクトA]      │
│        ⇄                     │
│  [数値入力B] [単位セレクトB]      │
└──────────────────────────────┘
```

- カテゴリはタブ型ボタン（`aria-pressed` で選択状態を表現）
- 入力欄は `<input type="text" inputmode="decimal">` を使い、数値判定は JS 側で行う（`type="number"` だと不正入力の検知やプレースホルダー制御が難しいため）
- モバイル幅でも崩れないよう flexbox の縦積みレスポンシブにする

### データモデル

各カテゴリは「基準単位への変換関数ペア」を持つ単位の配列として定義する。温度のオフセット変換を吸収するため、係数ではなく関数で持つ:

```js
const CATEGORIES = {
  length: {
    label: '長さ',
    units: [
      { id: 'm',  label: 'メートル (m)',      toBase: v => v,          fromBase: v => v },
      { id: 'cm', label: 'センチメートル (cm)', toBase: v => v / 100,    fromBase: v => v * 100 },
      // mm, km, in, ft, ...
    ],
  },
  weight: { /* 基準: kg。mg, g, kg, t, lb, oz */ },
  temperature: {
    label: '温度',
    units: [
      { id: 'c', label: '摂氏 (℃)', toBase: v => v,                 fromBase: v => v },
      { id: 'f', label: '華氏 (℉)', toBase: v => (v - 32) * 5 / 9,  fromBase: v => v * 9 / 5 + 32 },
      { id: 'k', label: 'ケルビン (K)', toBase: v => v - 273.15,     fromBase: v => v + 273.15 },
    ],
  },
};
```

### 主要関数

- `selectCategory(categoryId)` — カテゴリ切り替え。両側の単位セレクトを再構築し、入力欄をクリアする
- `convert(value, fromUnit, toUnit)` — `fromUnit.toBase(value)` → `toUnit.fromBase(base)` の2段変換で結果を返す純粋関数
- `formatResult(num)` — 浮動小数点誤差を丸める表示整形（`Number(num.toPrecision(12))` で 0.30000000000000004 のような誤差を吸収し、指数表記を避けつつ文字列化）
- `handleInput(sourceSide)` — input イベントハンドラ。入力値を `parseFloat` 相当で検証し、数値なら対向欄に変換結果を書き込み、空・非数値なら対向欄を空にする（エラーを出さない）
- 単位セレクト変更時も同じ変換ロジックを再実行し、表示を最新化する

### 検証

- 実装後に `npm run verify` を実行し、受け入れ条件を手元でブラウザ確認（各カテゴリの代表値・境界値・非数値入力）してから完了とする
