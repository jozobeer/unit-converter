{
  "name": "unit-converter",
  "private": true,
  "scripts": {
    "verify": "node scripts/verify.mjs",
    "deploy": "wrangler deploy",
    "test": "playwright test"
  },
  "devDependencies": {
    "wrangler": "^4",
    "@playwright/test": "1.62.1"
  }
}
